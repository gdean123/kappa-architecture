ensure_formula_installed() {
    FORMULA_TO_INSTALL=$1
    
    if brew ls --versions ${FORMULA_TO_INSTALL} > /dev/null; then
        echo "The formula ${FORMULA_TO_INSTALL} is already installed"
    else
        brew install "${FORMULA_TO_INSTALL}"
    fi
}