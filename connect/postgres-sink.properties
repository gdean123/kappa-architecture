name=consumer-sink
connector.class=io.confluent.connect.jdbc.JdbcSinkConnector
tasks.max=1
topics=word_counts
connection.url=jdbc:postgresql://localhost:5432/consumer_development
auto.create=true
pk.mode=record_key
insert.mode=upsert
